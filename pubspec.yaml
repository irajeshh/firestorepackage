name: firestorepackage
description: "A new Flutter package project."
version: 0.0.1
publish_to: none
homepage:

environment:
  sdk: ^3.3.4

dependencies:
  flutter:
    sdk: flutter
  # LOCAL
  extensions:
    path: ../extensions
  models:
    path: ../models
  # FIREBASE
  firebase_core: 4.1.0
  firebase_core_web: 3.1.0
  firebase_core_platform_interface: 6.0.0
  cloud_firestore: 6.0.1

dev_dependencies:
  lintingpackage:
      path: ../lintingpackage